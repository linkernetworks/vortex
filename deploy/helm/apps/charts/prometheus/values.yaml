global:
  environment: develop

replicaCount: 1

config:
  scrapeInterval: 5s

service:
  ports:
    port: 9090
    targetPort: 9090
    nodePort: 30003


# dependenvies
cadvisor:
  config:
    housekeeping_interval: 10s
kube-state-metrics:
  image:
    tag: v1.3.1
node-exporter:
  image:
    tag: 1.0.1

